#include <fstream>
#include <iostream>
#include <sstream>
#include <string>
#include <vector>
#include <unordered_map>
#include <unordered_set>
#include "project3.h"

using namespace std;

void readCSV(string filename, hashTbl &data, trie &data2) {
    // parses the csv and stores into hash table and trie
}
